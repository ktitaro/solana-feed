# How to run locally

```sh
# Setup testnet and deploy contract
$ cd dapp
$ solana config set --url devnet
$ anchor deploy

# Run website to interact with contract
$ cd website
$ yarn install
$ yarn run dev
```
